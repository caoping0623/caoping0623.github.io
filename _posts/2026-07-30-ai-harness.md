---
title: "AI 里的 Harness：别再只调模型，先把「测试架」搭好"
date: 2026-07-30
categories: [人工智能]
tags: [Harness, Agent, 测试, SIL, HIL, 车载, MCP, Skills, Cursor, 工程实践]
excerpt: "Harness 不是玄学新词。对车载工程师来说，它很像 SIL/HIL 里的测试架：模型是被测件，harness 是周围那一整套夹具、激励、观测、权限和回放。本文用直白例子讲清 Agent Harness、Eval Harness 和 Harness Engineering，并说明车载开发里哪些地方现在就能用、哪些地方必须加安全闸门。"
---

> 如果你做过车载开发，你一定见过 **test harness（测试架）**：把被测软件放进去，外面接仿真器、喂信号、采日志、判通过/失败。
>
> AI 圈子最近一年反复提的 **Harness**，本质上是同一类东西——**不是模型本身，而是把模型包起来、让它能干活、能自检、能被管住的那一整套工程环境。**
>
> 这篇文章写给工程师，尤其是车载方向的同事。看完你应该能回答三件事：Harness 到底指什么、工程里怎么落地、车载场景能不能用、怎么用才不踩安全红线。

---

## 一、先忘掉「AI」，用你已经会的东西理解它

### 1.1 一个你一定做过的类比

假设你在做 **SIL（Software-in-the-Loop）**：

| 角色 | SIL 里是什么 | AI 里对应什么 |
| --- | --- | --- |
| **被测对象** | 控制器算法 / ECU 软件 | 大模型（LLM） |
| **环境 / 台架** | 车辆动力学模型、传感器仿真、CAN 总线仿真 | 仓库、文档、工具、浏览器、诊断接口 |
| **测试架（Harness）** | 激励注入、I/O 映射、断言、日志、复位、故障注入 | **工具调用、权限、记忆、校验环、审批门、观测与回放** |
| **判定** | 通过/失败、覆盖率、时序是否满足 | 任务是否完成、测试是否绿、是否越权 |

SIL/HIL 里你绝不会说：「算法够强就行，台架随便搭。」  
AI 里同理：**模型再强，没有 harness，它也只是会说话的计算器**；有了 harness，它才变成能在真实工程环境里做事的 Agent。

联合国大学（UNU）2026 年的报告把这件事说得很清楚：

> Harness 是模型与环境之间的**运行时层**：负责把模型输出变成工具调用、观测、记忆更新、审批、中断、恢复和可回滚动作。

一句话公式：

```text
Agent ≈ 模型 + Harness
```

换模型，能力可能变；**换 harness，可靠性和安全性往往变得更明显。**

### 1.2 为什么现在突然人人都在说 Harness

过去几年大家经历了三轮重点迁移：

| 阶段 | 大致时间 | 大家在卷什么 | 典型问题 |
| --- | --- | --- | --- |
| Prompt Engineering | 2022–2024 | 怎么把话说清楚 | 同一句话换个说法结果就飘 |
| Context Engineering | 2024–2025 | 给模型喂什么上下文 | 材料太多/太少/不相关 |
| **Harness Engineering** | 2025–2026 | **怎么让模型在系统里稳定、可控地行动** | 会做但会闯祸、做一半丢状态、不会自检 |

HashiCorp 联合创始人 **Mitchell Hashimoto** 在 2026 年 2 月的文章《My AI Adoption Journey》里给了最直白的工程定义：

> 每当发现 Agent 犯了一次错，就花时间工程化一个解法，让它**以后再也不犯同样的错**。他把这叫 **harness engineering**。

注意：这里「工程化」不是再骂模型一句「下次注意」，而是改环境——加规则、加脚本、加测试、加权限门。

### 1.3 先把三个容易混的词拆开

| 概念 | 它干什么 | 一句话 |
| --- | --- | --- |
| **Agent Harness（运行时架）** | 让模型能行动：工具、记忆、循环、权限、恢复 | **跑系统** |
| **Eval Harness（评测架）** | 用固定用例/指标重复打分，保证可比、可复现 | **打分数** |
| **Test Harness（经典测试架）** | 激励 + 观测 + 断言，验证软件行为 | **验软件** |

三者都叫 harness，因为结构相似：**把「被测/被驱对象」固定住，外面用夹具把输入输出和判定标准化。**  
本文重点讲 **Agent Harness**，评测架会单独一节带过，经典测试架用来当对照。

---

## 二、Agent Harness 里面到底有什么

把 harness 拆成六块积木。你可以把它想成「AI 版的台架机柜」：

<div class="mermaid">
graph TB
    M["🧠 模型<br/>只负责推理与提议动作"]

    subgraph H["🔧 Agent Harness"]
        C["上下文管道<br/>规则 / 文档 / 检索"]
        T["工具层<br/>读文件 / 跑命令 / MCP"]
        V["校验环<br/>编译 / 单测 / linter"]
        P["权限与审批<br/>沙箱 / 白名单 / 人工确认"]
        Mem["记忆与状态<br/>会话 / 项目记忆 / 轨迹"]
        O["观测<br/>日志 / Trace / 回放"]
    end

    E["🌍 环境<br/>代码仓 / CI / 文档 / 诊断数据"]

    M --> H
    H --> E
    E --> H
    H --> M

    style M fill:#c7ceea,color:#3d3556
    style H fill:#bee3db,color:#3d3556
    style E fill:#ffdac1,color:#3d3556
</div>

### 2.1 上下文管道（Context Pipeline）

决定模型「上班前能看到什么」：

- 项目规则：`AGENTS.md` / Cursor Rules / Skill
- 相关代码与文档检索
- 接口定义、编码规范、安全红线

没有它，模型就像新入职同事没看过架构图，上来就乱改。

### 2.2 工具层（Tools）

模型不能直接拧螺丝，只能**提议调用工具**。工具可以是：

- 读/写文件、跑终端命令、查 Git
- 浏览器、数据库、工单系统
- 通过 [MCP]({{ '/2026/07/28/function-calling-mcp-skills/' | relative_url }}) 暴露的标准化工具

这和 ECU 不能「直接想象」车速、必须通过传感器接口拿信号，是同一逻辑。

### 2.3 校验环（Verification Loop）——最关键的一块

Hashimoto 强调的重点就在这里：**给 Agent 快速、高质量的「你错了」信号。**

常见校验器：

- 编译 / 类型检查
- 单元测试 / 静态分析
- 格式化与 lint
- 自定义脚本（比如「禁止改安全相关文件」「差分必须带测试」）

没有校验环，Agent 容易「自我感觉良好地交付烂结果」。  
有了校验环，它像进了带仪表的台架：动作一出，立刻有红灯绿灯。

### 2.4 权限与审批（Guardrails）

决定哪些动作可以自动做，哪些必须人点头：

| 动作类型 | 典型策略 |
| --- | --- |
| 只读：搜代码、读日志 | 可自动 |
| 本地可逆改动：改业务代码 + 跑测 | 可自动或轻确认 |
| 高风险：`git push --force`、删库、改安全标定、写实车 | **必须人工审批 / 直接禁止** |

车载语境下，这一层几乎是硬需求。

### 2.5 记忆与状态（Memory / State）

长任务需要记住：做到哪一步了、上次失败原因、用户偏好、项目约定。  
UNU 报告里提到的常见失败，很多都是状态问题：中断后重复操作、上下文溢出后「失忆」、无法恢复。

### 2.6 观测与回放（Observability）

记录：调了什么工具、入参出参、花了多久、失败在哪一层。  
没有 trace，你只能抱怨「AI 不行」；有了 trace，你才能改 harness。

---

## 三、工程里怎么实际应用（详细例子）

下面几个例子故意写细，方便你对照自己团队现状。

### 3.1 例子一：编码 Agent 的「防重复踩坑」架（最通用）

**场景**：用 Cursor / Claude Code 改一个模块，它反复犯同一类错。

**坏做法**：每次聊天里骂它「别再这么写」。明天新会话，它照犯。

**Harness 做法**（Hashimoto 路线）：

1. **观察到错误**：Agent 总爱在车载工程里用宿主机路径写死配置，CI 挂掉。
2. **工程化一次**：
   - 在 `AGENTS.md` 加一条：「禁止硬编码绝对路径；配置必须走相对路径 + 环境变量。」
   - 加一个脚本 `scripts/check_no_abs_path.py`，扫描 diff。
   - 在规则里写：「改完必须跑 `python scripts/check_no_abs_path.py`」。
3. **下次**：Agent 若再犯，校验环立刻红灯，它自己改，而不是等你发现。

这就是 harness engineering 的「棘轮」：**错误变成永久夹具，而不是永久嘴炮。**

### 3.2 例子二：把 CI 当成「台架仪表」

很多团队已经有 CI，却没把它当成 Agent 的传感器。

**改造前**：

> 人让 AI 改代码 → AI 说完成了 → 人再去点 CI → 红了再回来骂 AI

**改造后（harness）**：

> Agent 改代码 → **自动**跑本地/远端检查（编译、单测、静态分析）→ 红则继续修 → 绿才允许宣称完成

伪流程：

```text
while not done:
    Agent 提议修改
    应用补丁
    运行: build + unit_test + lint
    if fail:
        把失败日志喂回 Agent   # 这就是传感器反馈
    else:
        生成 PR 摘要，等待人工 Review
```

对车载来说，本地至少先接上：

- 模块级编译
- GoogleTest / 现有单元测试
- 静态检查（如 MISRA 相关工具若已有流水线，就接进去）

**关键点**：反馈要快。台架信号延迟太大，控制环就会抖；Agent 同理。

### 3.3 例子三：Eval Harness——别用「感觉」换模型

**场景**：领导问：「换个更强的模型，诊断助手会不会更好？」

没有评测架，你只能靠几个人主观试。有了 **Eval Harness**，你可以：

1. 固定一批用例（比如 50 条真实脱敏诊断问答）
2. 固定评分方式（关键字段是否抽对、是否胡编 DTC、是否给出危险建议）
3. 同一 harness 配置下跑模型 A / B
4. 得到可复现分数

开源里最出名的是 EleutherAI 的 **lm-evaluation-harness**（偏学术基准）；工程团队更常见的是自建「黄金集 + 自动判分」。

记住对比：

| | Agent Harness | Eval Harness |
| --- | --- | --- |
| 目的 | 让系统能干活 | 让对比可复现 |
| 输入 | 真实任务环境 | 固定用例集 |
| 输出 | 产物/动作 | 分数/报告 |

两者常一起用：**用 agent harness 跑任务，用 eval harness 量好坏。**

### 3.4 例子四：权限分层——「能读总线日志，不能刷写 ECU」

假设你做一个「诊断辅助 Agent」：

| 工具 | 权限 |
| --- | --- |
| 解析 `.asc` / `.blf` CAN 日志 | ✅ 自动 |
| 查询内部 DTC 知识库 | ✅ 自动 |
| 生成可能原因列表 | ✅ 自动 |
| 生成刷写脚本 | ⚠️ 仅草稿，默认禁止执行 |
| 通过诊断仪下发会话/刷写 | ❌ 禁止，或强制双人审批 + 台架环境标识 |

这不是「不信任 AI」，而是和功能安全同样的思路：**危险动作与人/环境解耦，默认失败安全。**

### 3.5 例子五：和本站已有概念怎么拼

如果你读过 [Function Calling / MCP / Skills]({{ '/2026/07/28/function-calling-mcp-skills/' | relative_url }})：

| 零件 | 在 Harness 里扮演什么 |
| --- | --- |
| Function Calling | 模型「会不会填工具表单」 |
| MCP | 工具怎么标准化接入 |
| Skills / AGENTS.md | 操作手册与领域约束 |
| 权限 / 沙箱 / CI | 安全与校验环 |
| Trace / 日志 | 观测与改进依据 |

**Harness 是总装；MCP/Skills 是总装里的线束和 SOP。**

---

## 四、车载领域能不能用？能，而且很对口

结论先说：

> **能用，而且车载工程师其实比很多互联网同事更懂 harness——因为你们天天在搭 SIL/HIL。**  
> 但车载有一条硬边界：**量产安全相关路径上，Agent 只能辅助，不能绕过既有安全与质量体系自动闭环。**

### 4.1 你已经在用的「老 Harness」

| 车载概念 | 和 AI Harness 的对应 |
| --- | --- |
| MIL / SIL / HIL / VIL | 不同保真度的环境 + 夹具 |
| 故障注入 | 故意给坏观测，看系统是否稳健（对 Agent 也可做「脏日志/对抗提示」测试） |
| 需求追溯、测试用例、覆盖率 | Eval / 验证证据链 |
| 权限、诊断安全访问、刷写保护 | Guardrails |
| 记录仪、总线日志、HIL 报告 | Observability |

所以学 AI harness，不是另起炉灶，而是：**把同一套工程纪律套到「会说话的组件」上。**

### 4.2 车载里现在就能落地的 6 个场景（从易到难）

#### 场景 A：需求 / 变更说明 → 测试用例草稿（强烈推荐先做）

**痛点**：功能变更一来，手工补测试用例慢，还容易漏边界。

**Harness 设计**：

1. **输入**：变更说明、相关需求 ID、接口文档片段  
2. **工具**：只读检索需求库；写入仅限「测试用例草稿目录」  
3. **校验环**：
   - 每条用例必须含：前置条件 / 步骤 / 期望结果 / 需求 ID
   - 禁止出现「适当」「大概」这类不可判定词（可用脚本扫）
4. **人工**：测试工程师 Review 后入库

**详细对话示例（直白版）**：

> 你：根据 `REQ-ACC-012` 变更，生成 SIL 可执行级别的测试用例草稿，覆盖正常、超时、传感器丢失三类。  
> Agent：写出 12 条用例。  
> 脚本：发现 3 条缺「期望结果」→ 打回。  
> Agent：补全。  
> 你：通过，再人工精修进 TestRail。

这比让 Agent「直接改量产代码」安全一个数量级。

#### 场景 B：CAN / 以太网日志辅助分析

**痛点**：实车/台架问题复现后，工程师要在海量帧里找「谁先异常」。

**Harness 设计**：

1. 工具：`parse_can_log`、按信号过滤、对齐时间轴、导出片段  
2. 知识：DTC 含义、信号字典（DBC）只读接入  
3. 强制输出模板：时间线 → 可疑信号 → 假设 → **需要人工确认的点**  
4. 禁止：根据日志直接给出「已确认根因并自动提维修工单」一类越权动作

**价值**：加速定位，不替代责任判定。

#### 场景 C：模块代码助手（应用层 / 工具链），带车规约束

**痛点**：AI 写的代码「能跑」但不合规：动态内存、递归、裸 `printf`、忽略返回值。

**Harness 设计（很车载）**：

在规则里写死，例如：

```text
本仓库为车载嵌入式 C 工程：
1. 禁止在热路径使用动态内存分配
2. 所有外部调用必须检查返回值
3. 修改安全相关目录（Safety/）前必须停下等待人工确认
4. 每次改动后必须运行: west build -b <board> 与 ctest -R <module>
5. 提交信息必须关联 Issue ID
```

再配上：

- 编译 + 单测作为硬校验
- `Safety/` 目录写入需要审批门
- diff 扫描禁用 API 列表

这时 Agent 不是「更聪明」，而是「被夹具夹住后更可靠」。

#### 场景 D：把 SIL 场景生成自动化一点

**痛点**：回归场景库建设慢，边界工况想不全。

**做法**：

- Agent 根据功能描述生成「场景清单 + 参数范围」草稿
- 校验：每个场景必须映射到需求 ID；危险场景必须标注预期安全响应
- 真正跑仿真仍走现有 SIL 流水线（CarMaker / Prescan / 自研仿真等）
- Agent **不直接**改仿真器安全阈值，只提 PR 式变更

这和「GenAI 辅助生成测试计划，再在 SIL/HIL 大规模执行」的工业实践是同一方向。

#### 场景 E：工具链 / 脚本 / CI 卫生（最适合练手）

车载仓库常有：一堆 Python 转换脚本、DBC 处理、日志切割、版本打包。

这些**非行车安全路径**是 Agent + Harness 的最佳练兵场：

- 改脚本 → 跑单测 → 用样例日志回归 → 出 PR
- 失败就回灌日志，直到绿

先在这里把 harness 跑顺，再考虑靠近控制器代码。

#### 场景 F：知识问答（架构、接口、历史问题）

接内部 Wiki / 代码仓检索，但：

- 答案必须带出处链接
- 无出处时必须说「未找到」
- 禁止把猜测写成规范结论

这属于轻 harness，但对新人 onboarding 很值。

### 4.3 一张「能不能上 Agent」的车载决策表

| 工作内容 | 建议 | 原因 |
| --- | --- | --- |
| 文档、用例草稿、日志解读、脚本工具 | ✅ 积极用 | 可逆、可审、收益高 |
| 应用层非安全模块 + 强测试校验 | ✅ 可用 | harness 能兜住大部分错 |
| 安全相关软件（ASIL 路径）、标定、刷写、实车控制 | ⚠️ 仅辅助，禁止自动闭环 | 责任与标准不允许黑盒自治 |
| 生成 ISO 26262 / ASPICE 证据并自动签字 | ❌ 不行 | 证据链与责任必须由组织流程承载 |

### 4.4 一个完整的车载小案例：从「翻车」到「夹具」

**背景**：团队让 Agent 改 BCM 里一个门控相关的应用模块。

**第一次翻车**：

1. Agent 改完宣称成功  
2. 本地没跑目标板编译  
3. CI 在 QNX 交叉编译挂了  
4. 还误改了旁边的诊断映射表

**按 harness engineering 改造**（一次性投入，长期收益）：

| 翻车点 | 加到 harness 的东西 |
| --- | --- |
| 没编译就说完成 | 规则：完成前必须跑 `./scripts/build_bcm.sh`；失败不得结束 |
| 乱改诊断表 | 路径守卫：`diag/` 默认只读；要改必须 `--approve-diag` |
| 不写测试 | 模板：相关模块变更必须新增/更新 `test_door_*.c` |
| 提交说明混乱 | 钩子：commit message 必须含 `JIRA-xxxx` |

**第二次**：

Agent 改代码 → 编译红 → 自修 → 单测红 → 自修 → 绿 → 生成 PR 说明（含风险点）→ **人做 Review**。

模型可能还是同一个。变强的是 harness。

这和你在 HIL 上加「缺相保护、超时保护、非法指令拦截」是同一类工程直觉。

### 4.5 车载落地的推荐路线（务实）

<div class="mermaid">
graph LR
    A["1. 选非安全路径<br/>脚本/文档/用例"] --> B["2. 写 AGENTS.md<br/>把踩坑写成规则"]
    B --> C["3. 接上编译+单测<br/>作为硬反馈"]
    C --> D["4. 加路径权限门<br/>Safety 目录隔离"]
    D --> E["5. 建黄金评测集<br/>换模型有分数"]
    E --> F["6. 再考虑靠近<br/>控制器代码辅助"]

    style A fill:#b5ead7,color:#3d3556
    style B fill:#bee3db,color:#3d3556
    style C fill:#ffdac1,color:#3d3556
    style D fill:#c7ceea,color:#3d3556
    style E fill:#e2f0cb,color:#3d3556
    style F fill:#ffd6e0,color:#3d3556
</div>

**不要一上来就让 Agent 碰刷写和实车。**  
先在工具链和文档侧把「错误→夹具」的闭环跑通，团队才会信这套方法。

---

## 五、怎么动手搭一个最小可用 Harness

哪怕只有你一个人，也可以从最小闭环开始：

### 5.1 最小四件套

1. **规则文件**：`AGENTS.md` 或 Cursor Rules（写清仓库禁忌与完成定义）  
2. **一个校验脚本**：例如 `make verify` = 编译 + 单测 + 自定义检查  
3. **权限边界**：哪些目录可写、哪些命令禁止  
4. **一次失败归档**：每次 Agent 翻车，沉淀 1 条规则或 1 个检查

### 5.2 「完成定义」要写成机器可执行的

坏的完成定义：

> 把功能做好。

好的完成定义：

```text
完成必须同时满足：
1) ./scripts/build.sh 退出码 0
2) ctest -R door_ 全部通过
3) scripts/check_no_todo_in_diff.py 通过
4) 输出变更说明，列出风险与未覆盖测试
否则不得声称完成。
```

Agent 是目标导向的：你给的目标含糊，它会走捷径；你给的目标可机检，它才被「夹」回正轨。

### 5.3 和 Cursor / Hermes / WorkBuddy 的关系

| 产品 | 你看到的 | 底下的 harness 味道 |
| --- | --- | --- |
| Cursor | 改代码的 Agent | 工具 + 规则 +（可配）权限与终端策略 |
| Hermes | 个人 Agent 框架 | Skills、MCP、定时、多端投递 |
| WorkBuddy | 办公桌面 Agent | 工作空间、连接器、Skill、结果验收 |

选产品是选「现成 harness」；**真正拉开差距的，是你有没有把领域约束和校验环写进去。**

---

## 六、常见误区

| 误区 | 纠正 |
| --- | --- |
| 「换更强模型就行」 | 很多故障在 harness：没工具、没校验、没权限、没记忆 |
| 「多写点 prompt 就等于 harness」 | Prompt 只是一层；缺脚本/测试/权限门时，prompt 挡不住重复犯错 |
| 「有 harness 就可以无人值守上实车」 | 车载安全路径必须保留人与流程；harness 是辅助加速，不是替代认证 |
| 「评测就是跑几个 demo」 | 没有固定用例与评分，换模型对比没有工程意义 |
| 「一次配完美」 | harness 是演化的：每次失败加一根「钢筋」 |

---

## 七、总结

### 7.1 记这三句就够

1. **Harness = 模型周围的工程夹具**：工具、上下文、校验、权限、记忆、观测。  
2. **Harness Engineering = 把每次翻车变成永久防护**，而不是永久抱怨。  
3. **车载不但能用，而且天然适配**——你们本就生活在 SIL/HIL 的 harness 文化里；把同样纪律用到 AI，收益最大、也最安全。

### 7.2 对车载同事的行动建议

- **本周**：挑一个非安全仓库（脚本/用例/文档），写下 `AGENTS.md` + `make verify`  
- **本月**：给「危险目录/危险命令」加门；攒 20～50 条黄金评测题  
- **本季**：再评估是否让 Agent 辅助应用层改码；安全相关仍保持人审 + 既有 V 模型证据链

### 7.3 最后

模型像「潜在很强的新同事」。  
Harness 像「岗位说明书 + 权限卡 + 工装夹具 + 下线检测」。

车载行业从来不缺对夹具的尊重——缺的只是把这套尊重，明确地用到 AI 上。

> 与其问「这个模型聪不聪明」，不如问「我们有没有给它一台合格的台架」。

---

*本文结合 2026 年围绕 Agent Harness / Harness Engineering 的公开讨论（含 Mitchell Hashimoto 的实践定义、UNU 对运行时层的界定），以及车载 SIL/HIL 工程常识整理。产品与标准细节请以你司流程与现行规范为准。*
